# Local development example only. Do not reuse this password in deployment.
POSTGRES_DB=backend
POSTGRES_USER=backend
POSTGRES_PASSWORD=backend-local-password
POSTGRES_PORT=5432

KAKAO_CLIENT_ID=your-kakao-rest-api-key
KAKAO_CLIENT_SECRET=your-kakao-client-secret
KAKAO_REDIRECT_URI=http://localhost:5173/auth/kakao/callback

# 눈으로 그리기 AI 채점 (SSAFY GMS, OpenAI 호환). 키가 비어 있으면 그림 인식이 비활성화된다.
GMS_API_KEY=your-gms-api-key
GMS_MODEL=gpt-4o

JWT_SECRET_KEY=
APP_CORS_ALLOWED_ORIGINS=
APP_PORT=8080
